import SwiftUI

// MARK: - Типографика SIGNAL
extension Font {
    static let signalLargeTitle = Font.system(.largeTitle, design: .default).weight(.thin)
    static let signalTitle = Font.system(.title, design: .default).weight(.ultraLight)
    static let signalTitle2 = Font.system(.title2, design: .default).weight(.thin)
    static let signalBody = Font.system(.body, design: .default).weight(.regular)
    static let signalCaption = Font.system(.caption, design: .monospaced).weight(.regular)
    static let signalCaption2 = Font.system(.caption2, design: .monospaced).weight(.light)
}
